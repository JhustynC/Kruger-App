<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Log in</title>
  <style>
    .google-login {
      font-size: 20px;
      /* Ajusta el tamaño del texto */
      font-weight: bold;
      /* Hace el texto más grueso */
      text-decoration: none;
      /* Elimina el subrayado */
      padding: 10px 20px;
      /* Añade espacio interno */
      border-radius: 5px;
      /* Bordes redondeados */
      display: inline-block;
      /* Necesario para el degradado */
      background: linear-gradient(90deg, #4285F4, #34A853, #FBBC05, #EA4335);
      background-clip: text;
      /* Aplica el degradado solo al texto */
      -webkit-background-clip: text;
      /* Para compatibilidad en navegadores WebKit */
      color: transparent;
      /* Hace el texto transparente para mostrar el degradado */
      transition: transform 0.2s;
      /* Añade una animación para el hover */
    }

    .google-login:hover {
      transform: scale(1.1);
      /* Aumenta ligeramente el tamaño al pasar el cursor */
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      flex-direction: column;
    }

    h1 {
      color: #333;
      text-align: center;
      margin-bottom: 2rem;
    }

    form {
      background-color: #fff;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
    }

    div {
      margin-bottom: 1rem;
    }

    label {
      display: block;
      font-size: 1rem;
      color: #555;
      margin-bottom: 0.5rem;
    }

    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 0.75rem;
      font-size: 1rem;
      border: 1px solid #ddd;
      border-radius: 4px;
      box-sizing: border-box;
    }

    button {
      width: 100%;
      padding: 0.75rem;
      background-color: #007bff;
      color: white;
      font-size: 1rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #0056b3;
    }

    a {
      display: block;
      text-align: center;
      margin-top: 1rem;
      color: #007bff;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <h1>Kruger App</h1>
  <form action="/auth/login-credentials" method="POST">
    <div>
      <label for="email">Email</label>
      <input type="email" name="email" id="email" required />
    </div>
    <div>
      <label for="password">Password</label>
      <input type="password" name="password" id="password" required />
    </div>
    <button type="submit">Log in</button>
  </form>
  <a href='/auth/google' class="google-login">Log in with Google</a>
</body>

</html>